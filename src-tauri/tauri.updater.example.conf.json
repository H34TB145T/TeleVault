{
  "bundle": {
    "createUpdaterArtifacts": true
  },
  "plugins": {
    "updater": {
      "pubkey": "REPLACE_WITH_TAURI_SIGNING_PUBLIC_KEY",
      "endpoints": [
        "https://updates.example.com/televault/{{target}}/{{arch}}/{{current_version}}"
      ]
    }
  }
}
